# Virtual environment — never commit this
venv/

# Data files — too large for Git, download via Kaggle API
data/
*.parquet
*.csv
*.zip

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Spark
spark_checkpoints/
derby.log
metastore_db/

# Credentials — never commit
.kaggle/
*.env
dashboard_data.json

# Mac system files
.DS_Store

# VS Code settings (optional — some prefer to commit these)
.vscode/
